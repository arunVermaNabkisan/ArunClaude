@page "/logout"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the controller endpoint that handles the actual sign-out
        Navigation.NavigateTo("/Account/Logout", forceLoad: true);
    }
}